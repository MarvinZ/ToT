import { DefaultLocaleCodes } from './default-locale-codes';
export declare class DefaultLocale extends DefaultLocaleCodes {
    value: string;
    private _value;
    build(languageCode: string, countryCode?: string, scriptCode?: string, numberingSystem?: string, calendar?: string): void;
    private parseValue();
}
