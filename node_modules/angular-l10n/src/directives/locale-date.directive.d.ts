import { ElementRef, Renderer } from '@angular/core';
import { LocaleService } from '../services/locale.service';
import { BaseDirective } from '../models/utils/base-directive';
export declare class LocaleDateDirective extends BaseDirective {
    locale: LocaleService;
    protected el: ElementRef;
    protected renderer: Renderer;
    pattern: string;
    private defaultPattern;
    constructor(locale: LocaleService, el: ElementRef, renderer: Renderer);
    protected setup(): void;
    protected replace(): void;
}
